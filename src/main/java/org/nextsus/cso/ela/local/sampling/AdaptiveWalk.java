package org.nextsus.cso.ela.local.sampling;

public class AdaptiveWalk {
    // PHC
    /*
    individual = newSolution
    do
        newIndividual = neighbor(individual);
        if newIndividual is better than individual then
            individual = newIndividual
        end if
    while (individual is not improved);
    return individual
     */

    /*
    s = new Solution
    stop = false
    while !stop
        last = f(s)
        for n in neighbors
            if f(n) > f(s)
                s = n

        if f(s) == last
            stop = true

     return s       // PLO
     */
}
